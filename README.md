1.Create a virtual environment (Optional but reccomended) python -m venv venv (Powershell) venv\Scripts\activate source venv/bin/activate (Mac/Linux) 
2.Install requirements.txt pip install -r requirements.txt 
python -m pip list (Check requirements)
3.Run python data_loader.py ONE TIME to Create + FILL the DB if the FILE IS MISSING
4.Start the GUI
python frontend.py

